# Schema

Human-readable notes only — wakil no longer reads this file. Page shape and
frontmatter come from the entity-schema catalog (`schema/entities/*.yaml`);
see `docs/adr/0011-retire-schema-md-dependency.md`.
